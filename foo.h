#ifndef ALPHA_FOO_H
#define ALPHA_FOO_H

int myadd(int a, int b);

int fact(int n);

long long int fib(int n);
long long int fibm(int n);

class Foo {
public:
    int add (int a ,int b);
};

#endif //ALPHA_FOO_H
